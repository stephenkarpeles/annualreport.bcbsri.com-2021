import * as React from "react"
import "./header.css"

// markup
const Header = () => {
  return (
    <header className="header">
      <button>Menu</button>
    </header>
  )
}

export default Header
